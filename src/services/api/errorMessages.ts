/**
 * If the api uses the reference standard in the response,
 * you could specify the references here with custom messages.
 */
const errorMessages = {
  default: 'Something went wrong. Try again later or contact us.',
};

export default errorMessages;
